{
  "scripts": {
    "start": "npm run build && npm run copy && node dist/express.js",
    "build": "tsc",
    "copy": "cp src/*.html dist/"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "express": "^4.21.2",
    "groq-sdk": "^0.9.1",
    "ts-node": "^10.0.0",
    "typescript": "^4.5.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0"
  }
}
